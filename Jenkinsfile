@Library('vivarium_build_utils') _

pipeline {
    agent any
    
    stages {
        stage('Multi-Multibranch Pipeline') {
            steps {
                monorepo()
            }
        }
    }
}